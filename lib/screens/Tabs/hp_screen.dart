import 'package:flutter/material.dart';

class HPMeterScreen extends StatelessWidget {
  const HPMeterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'HP Meter Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
